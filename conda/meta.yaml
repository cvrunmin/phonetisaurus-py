package:
  name: "phonetisaurus-py"
  version: 0.3

source:
  path: ..

build:
  number: 0
  script: python -m pip install .

requirements:
  build:
    - {{ compiler('cxx') }}

  host:
    - python
    - pip
    - pybind11 >=2.6.0
    - openfst

  run:
    - python
    - openfst