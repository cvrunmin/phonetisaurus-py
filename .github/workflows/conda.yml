name: Conda

on: 
  workflow_dispatch:
    inputs:
      should_publish:
        description: 'Should publish to Anaconda'
        required: false
        type: boolean
  push:
    tags:
      - '*'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-latest, windows-2019, macos-latest]
        python-version: ["3.7", "3.8", "3.9", "3.10"]

    runs-on: ${{ matrix.platform }}

    # The setup-miniconda action needs this to activate miniconda
    defaults:
      run:
        shell: "bash -l {0}"

    steps:
      - uses: actions/checkout@v3

      - name: Get conda
        uses: conda-incubator/setup-miniconda@v2.1.1
        with:
          python-version: ${{ matrix.python-version }}
          channels: conda-forge

      - name: Prepare
        run: conda install conda-build conda-verify
        
      - name: Build
        run: conda build conda -c conda-forge --no-anaconda-upload
        
      - name: Install
        run: conda install -c ${CONDA_PREFIX}/conda-bld/ phonetisaurus-py

      - name: Find and export artifact
        uses: actions/upload-artifact@v3
        with:
          path: ${{ env.CONDA_PREFIX }}/conda-bld/*/phonetisaurus-py**.tar.bz2
      - name: Upload to Anaconda (win-64)
        if: ${{ (github.event_name == 'push' || inputs.should_publish) && runner.os == 'Windows' }}
        run: anaconda upload ${CONDA_PREFIX}/conda-bld/win-64/*.tar.bz2
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_TOKEN }}
      - name: Upload to Anaconda (linux-64)
        if: ${{ (github.event_name == 'push' || inputs.should_publish) && runner.os == 'Linux' }}
        run: anaconda upload ${CONDA_PREFIX}/conda-bld/linux-64/*.tar.bz2
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_TOKEN }}
      - name: Upload to Anaconda (osx-64)
        if: ${{ (github.event_name == 'push' || inputs.should_publish) && runner.os == 'macOS' }}
        run: anaconda upload ${CONDA_PREFIX}/conda-bld/osx-64/*.tar.bz2
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_TOKEN }}
